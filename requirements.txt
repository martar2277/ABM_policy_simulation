# Core dependencies
pyyaml>=6.0

# LLM integrations (optional - only needed if using real LLMs)
openai>=1.0.0  # For OpenAI GPT models
anthropic>=0.8.0  # For Anthropic Claude models

# Data analysis (optional)
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Development (optional)
pytest>=7.4.0
pytest-cov>=4.1.0
